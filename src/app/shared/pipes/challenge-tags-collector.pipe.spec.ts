import { ChallengeTagsCollectorPipe } from './challenge-tags-collector.pipe';

describe('CollectChallengeTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChallengeTagsCollectorPipe();
    expect(pipe).toBeTruthy();
  });
});
