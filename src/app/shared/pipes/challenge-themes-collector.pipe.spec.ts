import { ChallengeThemesCollectorPipe } from './challenge-themes-collector.pipe';

describe('CollectChallengeTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChallengeThemesCollectorPipe();
    expect(pipe).toBeTruthy();
  });
});
