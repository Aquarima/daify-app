import { OrderByChallengePipe } from './order-by-challenge.pipe';

describe('OrderByChallengePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByChallengePipe();
    expect(pipe).toBeTruthy();
  });
});
