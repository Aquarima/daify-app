import { CustomInputFocusDirective } from './custom-input-focus.directive';

describe('CustomInputFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomInputFocusDirective();
    expect(directive).toBeTruthy();
  });
});
